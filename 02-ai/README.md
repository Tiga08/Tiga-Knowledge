# 02-ai

AI、CV、LLM、模型部署与推理优化。

## 现有目录

| 目录 | 说明 |
|------|------|
| `agent/` | Agent 原理、框架与应用 |
| `computer-vision/` | 图像分类、检测、分割、OCR、跟踪等，按任务建子目录（如 `object-detection/`） |

## 推荐分类（按需创建）

`deep-learning` · `llm` · `multimodal` · `model-deployment` · `model-compression` · `inference-acceleration` · `data-engineering`
